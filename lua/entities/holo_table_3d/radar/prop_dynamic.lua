-- prop_dynamic map dressing is owned by cl_map.lua now. This stub keeps
-- existing AddCSLuaFile / radar loader behaviour stable without registering
-- a per-frame radar drawer.
RADAR.Name = 'prop_dynamic'
